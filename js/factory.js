const factoryTemplate = (function () {
    const templates = {};

    return {
        getTemplate: (template) => {
            switch (template) {
                case "main":
                case "news":
                case "review":
                case "login":
                case "register":
                default:

            }
        }
    }
})();